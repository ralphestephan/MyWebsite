import type {Plugin} from "ajv"
import getDef from "ajv-keywords/src/definitions/oneRequired"

const oneRequired: Plugin<undefined> = (ajv) => ajv.addKeyword(getDef())

export default oneRequired
module.exports = oneRequired
