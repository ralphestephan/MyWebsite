import type {Vocabulary} from "ajv/lib/types"
import dynamicAnchor from "ajv/lib/vocabularies/dynamic/dynamicAnchor"
import dynamicRef from "ajv/lib/vocabularies/dynamic/dynamicRef"
import recursiveAnchor from "ajv/lib/vocabularies/dynamic/recursiveAnchor"
import recursiveRef from "ajv/lib/vocabularies/dynamic/recursiveRef"

const dynamic: Vocabulary = [dynamicAnchor, dynamicRef, recursiveAnchor, recursiveRef]

export default dynamic
