import type {CodeKeywordDefinition} from "ajv/lib/types"
import {dynamicRef} from "ajv/lib/vocabularies/dynamic/dynamicRef"

const def: CodeKeywordDefinition = {
  keyword: "$recursiveRef",
  schemaType: "string",
  code: (cxt) => dynamicRef(cxt, cxt.schema),
}

export default def
